# auth info
USER=admin
PASSWORD=password
EMAIL=email@email.com

# db server config
DB_PORT=5432
DB_DATA_DIR=./db_data

# strapi config
STRAPI_DB_NAME=strapi
STRAPI_APP_DIR=./app

# encrypt config
ENCRYPT_DIR=./letsencrypt

# jenkins
JENKINS_DATA_DIR=./jenkins

# domain and url
TEST_DOMAIN=test.threegems.net
# url format: http://${service name in the docker-compose.yaml}:${service exposed port in the docker-compose.yaml}
# more about docker network see https://docs.docker.com/compose/networking/
TEST_URL=http://server:1337
JENKINS_DOMAIN=jenkins.threegems.net
JENKINS_URL=http://jenkins:8080